const pageStyle = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  textAlign: 'center',
  width: '100%',
  p: 5
};

const formStyle = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  gap: 5
};

const listBoxStyle = {
  display: 'flex',
  flexDirection: 'column',
  gap: 3
};

export { pageStyle, formStyle, listBoxStyle };
